export type IResponse = {
    postId: number,
    id: number,
    name: string,
    email: string,
    body: string
}

export type IDropdownValue = {
    name: string;
    email: string;
    body: string;
    id: number;
}
